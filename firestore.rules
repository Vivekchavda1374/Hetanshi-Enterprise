rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // -----------------------------------------------------------------------
      // DEVELOPMENT RULES
      // -----------------------------------------------------------------------
      // Currently allowing ALL reads and writes because the app does not yet 
      // implement real Firebase Authentication (LoginScreen is hardcoded).
      //
      // TODO: Once you implement proper FirebaseAuth.signInWithEmailAndPassword,
      // change this detailed rule to:
      // allow read, write: if request.auth != null;
      // -----------------------------------------------------------------------
      
      allow read, write: if true;
    }
  }
}
